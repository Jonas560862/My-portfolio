services:
  - type: web
    name: my-portfolio
    runtime: python
    pythonVersion: 3.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: "8080"
    autoDeploy: true
    healthCheckPath: /
