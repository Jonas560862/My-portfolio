services:
  - type: web
    name: my-portfolio
    runtime: python
    pythonVersion: 3.9
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "8080"
    autoDeploy: true
    healthCheckPath: /
